{
  "name": "vue-lib",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write ./packages/**",
    "build": "pnpm -r run build",
    "publish": "pnpm -r publish --access public --no-git-checks --filter ./packages/shared"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.4.2",
    "pnpm": "^9.11.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}